# Copy to backend/.env — used when running the app on the host (npm run start:dev)
# and by the Prisma CLI (migrate/generate/studio).
# Host is localhost: Postgres is reached through the port published by compose.
DATABASE_URL="postgresql://example_user:example_password@localhost:5432/example_db_name?schema=public"
PORT=3000
